apiVersion: v1
kind: Service
metadata:
  name: {{ include "cleaning.fullname" . }}
  labels:
    {{- include "cleaning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cleaning.selectorLabels" . | nindent 4 }}

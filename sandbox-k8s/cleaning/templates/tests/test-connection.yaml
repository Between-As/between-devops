apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cleaning.fullname" . }}-test-connection"
  labels:
    {{- include "cleaning.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cleaning.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
